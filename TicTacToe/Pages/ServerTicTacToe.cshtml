@page
@model TicTacToe.Pages.ServerTicTacToeModelModel
@{
    ViewData["Title"] = "ServerTicTacToeModel";
}

<h2>ServerTicTacToe</h2>

<p>@Model.Turn's Turn</p>
<form method="post">
    <div class="Board">
        @for (int y = 0; y < 3; y++)
        {
            <div class="row">
                @for (int x = 0; x < 3; x++)
                {
                    @if (Model.Board[x, y] == null)
                    {
                        <input type="radio" name="move" class="cell" />
                    }
                    else
                    {
                        <input type="text" name="cell-@x-@y" value="@Model.Board[x, y]" class="cell" readonly />
                        
                    }
                }
            </div>
        }
    </div>
    <input type="hidden" name="turn" value="@Model.Turn" />
    <input type="hidden" name="state" value="@Model.State" />
    <input type="submit" value="Make a Move"/>
</form>
